#include "pile.hpp"
template <class TYPE>
class listePile {
private:
	stack<TYPE> _gauche;
	stack<TYPE> _droite;
public:
	... // Codez, ici, les méthodes habituelles d’une liste implicite
	 // en vous inspirant de celles fournies dans un des numéros
	 // précédents de ce document
};